title: Advanced Python Programming
description: Economics Master - Python Course
baseurl: "/advanced_programming_python"
url: "https://knuxv.github.io"

markdown: kramdown
highlighter: rouge
theme: minima

plugins:
  - jekyll-feed
  - jekyll-sitemap

minima:
  skin: auto
  social_links:
    github: KnuxV

# Optional: show selected pages in the header nav
header_pages:
  - index.md

# Force all pages to use our custom default layout
defaults:
  - scope:
      path: ""
      type: "pages"
    values:
      layout: "default"
  - scope:
      path: "lessons"
      type: "pages"
    values:
      layout: "default"
  - scope:
      path: "exercices"
      type: "pages"
    values:
      layout: "default"
